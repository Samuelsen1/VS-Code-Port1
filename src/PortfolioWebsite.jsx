import React from 'react';

// ... other imports and code

function PortfolioWebsite() {
    return (
        <div>
            {/* Other components */}

            {/* Adjusted Project Links */}
            <a key={index} href={project.link} target="_blank" rel="noopener noreferrer" className="rounded-2xl transition overflow-hidden group block flex flex-col relative shadow-lg hover:shadow-2xl" style={{width: '100%'}}>
                {/* Project Content */}
            </a>

            {/* Repeat similarly for other project links */}

        </div>
    );
}

export default PortfolioWebsite;